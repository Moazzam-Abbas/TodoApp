export default class ITodoAppTodoRepository {

    constructor() {
        throw new Error('Cannot instantiate an interface');
    }

    createTodoItem = (Todo) => { };
    findAllTodoItem = () => { };
    UpdateTodoItem = (Todo) => { };
    deleteTodoItem = (Todo) => { };

}
